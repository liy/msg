<%= form_for @entry, :class => "form-inline" do |f| %>
	<%= f.text_area :content %>
	<%= f.submit :Publish, :id => "submit-text-btn", :class => "btn" %>
	<span class="label">⌘ + enter</span> <span class="label">ctrl + enter</span>
<% end %>